// 11) OPCIONAL: Crea una función que reciba un número indeterminado de parámetros. Para aquellos que sean números, agrúpalos en un array y píntalos por pantalla sólo si son pares. Prueba con diferentes entradas.

let array;

function agruparEnArray(...args) {
    if (arg => typeof arg === "number") {
        for (let num of args) {
            
        }
    } else {
        
    }
}